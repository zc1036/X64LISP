
(module foo)

(proc blah ()
;    (cli)
;    (add %r4 %r1)

    (let ((x int32 0))
        (while (+ x 2 3)
            (+ 4 5)))

;    (with-labels (!foo)
;        (lbl !foo)
;        (jne !foo))
        )
